---
name: cto
description: Switch to the CTO role.
---

**FIRST action (before reading anything): use the Write tool to write `cto` to `.hats/role`.**

Then activate the Hats CTO profile. If the host provides the `hats:cto` agent,
switch to it. Otherwise read `../../agents/cto.md` relative to this `SKILL.md`
and follow that profile in the current agent. Do NOT accept any task or context
yet — run the activation status check and wait for the human.

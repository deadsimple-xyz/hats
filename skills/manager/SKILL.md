---
name: manager
description: Switch to the Manager role.
---

**FIRST action (before reading anything): use the Write tool to write `manager` to `.hats/role`.**

Then activate the Hats Manager profile. If the host provides the `hats:manager`
agent, switch to it. Otherwise read `../../agents/manager.md` relative to this
`SKILL.md` and follow that profile in the current agent. Do NOT accept any task
or context yet — run the activation status check and wait for the human.

---
name: developer
description: Switch to the Developer role.
---

**FIRST action (before reading anything): use the Write tool to write `developer` to `.hats/role`.**

Then activate the Hats Developer profile. If the host provides the
`hats:developer` agent, switch to it. Otherwise read
`../../agents/developer.md` relative to this `SKILL.md` and follow that profile
in the current agent. Do NOT accept any task or context yet — run the
activation status check and wait for the human.

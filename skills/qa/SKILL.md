---
name: qa
description: Switch to the QA role.
---

**FIRST action (before reading anything): use the Write tool to write `qa` to `.hats/role`.**

Then activate the Hats QA profile. If the host provides the `hats:qa` agent,
switch to it. Otherwise read `../../agents/qa.md` relative to this `SKILL.md`
and follow that profile in the current agent. Do NOT accept any task or context
yet — run the activation status check and wait for the human.

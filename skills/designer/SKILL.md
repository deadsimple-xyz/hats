---
name: designer
description: Switch to the Designer role.
---

**FIRST action (before reading anything): use the Write tool to write `designer` to `.hats/role`.**

Then activate the Hats Designer profile. If the host provides the
`hats:designer` agent, switch to it. Otherwise read `../../agents/designer.md`
relative to this `SKILL.md` and follow that profile in the current agent. Do
NOT accept any task or context yet — run the activation status check and wait
for the human.
